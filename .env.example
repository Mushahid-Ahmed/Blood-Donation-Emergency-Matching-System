# Database
DATABASE_URL="postgresql://appuser:securepassword@localhost:5433/bloodmatch?schema=public"

# NextAuth
AUTH_SECRET="changeme-generate-a-real-secret-with-npx-auth-secret"
NEXTAUTH_URL="http://localhost:3000"

# OpenAI (optional — AI features degrade gracefully if missing)
OPENAI_API_KEY=""

# App Config Defaults (also configurable via Admin panel / SystemConfig table)
DEFAULT_COOLDOWN_DAYS=56
DEFAULT_REQUEST_EXPIRY_HOURS=48
